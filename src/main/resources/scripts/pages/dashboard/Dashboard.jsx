import Button from "Common/components/Button/Button";
import React from "react";

export const Dashboard = () => (
    <div className="container">
        Welcome on dashboard
        <Button variant="primary" onClick={() => {}}>przycisk</Button>
    </div>
);
